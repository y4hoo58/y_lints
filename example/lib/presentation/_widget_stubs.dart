// Minimal Flutter-like stubs so the example compiles without depending on
// flutter. Real projects will import `package:flutter/widgets.dart`.
abstract class StatelessWidget {
  const StatelessWidget();
}

abstract class StatefulWidget {
  const StatefulWidget();
}
